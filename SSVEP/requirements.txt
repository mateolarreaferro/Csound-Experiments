# SSVEP 2-Choice BCI System - Requirements
# Install with: pip install -r requirements.txt

# Core scientific computing
numpy>=1.21.0
scipy>=1.7.0
scikit-learn>=1.0

# Signal processing and visualization
matplotlib>=3.5.0

# EEG/BCI hardware interface
brainflow>=5.13.0
pylsl>=1.16.0  # Lab Streaming Layer for OpenBCI GUI
# Note: On macOS, LSL library must be installed first:
#   brew install labstreaminglayer/tap/lsl
#   python fix_lsl.py  # Copies library to venv

# Visual stimulus presentation
pygame>=2.1.0  # Primary choice for simple graphics

# Optional: PsychoPy for advanced stimulus features
# Note: PsychoPy has complex dependencies and may have installation issues
# Uncomment the following lines if you need PsychoPy:
# psychopy>=2023.1.0
# PyQt5>=5.15.0

# Development and testing (optional)
pytest>=7.0.0
ipython>=8.0.0
